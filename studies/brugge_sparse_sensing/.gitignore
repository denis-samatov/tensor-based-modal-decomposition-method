# Study outputs are small, derived, and intended to be versioned.
!*.csv
!*.json
!outputs/
!run_logs/
!*.log
rerun_verification/
!*.npz
outputs/parts/
*.pkl
.claude/

# PNG previews are regenerated from canonical figures; publication PDFs remain tracked.
figures/*.png
